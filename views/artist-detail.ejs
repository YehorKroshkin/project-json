<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= artist.name %> - Details</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div class="container">
    <h1 class="artist-name"><%= artist.name %></h1>

    <!-- Контейнер для деталей артиста -->
    <div class="artist-details">
      <!-- Карточка артиста -->
      <div class="artist-card">
        <!-- Изображение артиста -->
        <img src="<%= artist.imageUrl %>" alt="<%= artist.name %>" class="artist-image">
        <!-- Информация о артисте -->
        <div class="artist-info">
          <p><strong>Genre:</strong> <%= artist.genre %></p>
          <p><strong>Age:</strong> <%= artist.age %></p>
          <p><strong>Birthdate:</strong> <%= artist.birthDate %></p>
          <p><strong>Instruments:</strong> <%= artist.instruments.join(', ') %></p>
          <p><strong>Description:</strong> <%= artist.description %></p>
          <p>
  <strong>Record Label:</strong>
  <a href="/labels/<%= artist.recordLabel.id %>"><%= artist.recordLabel.name %></a>
</p>

        </div>
      </div>
    </div>

    <!-- Кнопка возврата -->
    <a href="/" class="back-btn">← Back to Artists List</a>
  </div>
</body>
</html>

