<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= label.name %> - Label Details</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div class="container">
    <h1 class="artist-name"><%= label.name %></h1>

    <!-- Контейнер для деталей лейбла -->
    <div class="artist-details">
      <!-- Карточка лейбла -->
      <div class="artist-card">
        <!-- Логотип лейбла -->
        <img src="<%= label.labelLogoUrl %>" alt="<%= label.name %>" class="artist-image">
        
        <!-- Информация о лейбле -->
        <div class="artist-info">
          <p><strong>Founded:</strong> <%= label.foundedYear %></p>
          <p><strong>Founder:</strong> <%= label.founder %></p>
          <p><strong>Headquarters:</strong> <%= label.headquarters %></p>
        </div>
      </div>
    </div>

    <!-- Кнопка возврата -->
   <div class="button-row">
  <div class="btn-left">
    <button onclick="window.history.back()" class="back-btn">← Back to Artist </button>
  </div>
  <div class="btn-center">
    <a href="/" class="back-btn">Back to Artists List</a>
  </div>
  <div class="btn-right">
    <a href="/labels" class="back-btn"> Back to Labels List →</a>
  </div>
</div>



  </div>
</body>
</html>

